options:
  byRole :
    public:
    - /about
    authenticated:
    - /users
    developer:
    - /dev
    - /inbox
message: Should handle additional roles based routes
expected:
  home: /
  auth:
      pages:
        login: /auth
        logout: /auth/logout
      endpoints:
        signIn: /auth/signin
        session: /auth/session
  # login: /auth
  # logout: /auth/logout
  # endpoints:
  #   signIn: /auth/signin
  #   sessionCookie: /auth/session
  byRole:
    public:
    - /about
    - /auth
    authenticated:
    - /
    - /users
    developer:
    - /
    - /dev
    - /inbox

data:
  - roles: ""
    routes:
    - /about
    - /auth
    message: Should set allowed routes for "public"
  - roles: authenticated
    routes:
    - /
    - /about
    # - /auth
    - /auth/logout
    - /auth/session
    - /users
    message: Should set allowed routes for "authenticated"
  - roles: developer
    routes:
    - /
    - /about
    - /auth/logout
    - /auth/session
    - /dev
    - /inbox
    message: Should set allowed routes for "developer"
  - roles:
    - developer
    - authenticated
    routes:
    - /
    - /about
    # - /auth
    - /auth/logout
    - /auth/session
    - /dev
    - /inbox
    - /users
    message: Should set allowed routes for "developer" & "authenticated"
